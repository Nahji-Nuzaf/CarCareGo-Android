<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/brand_navy_dark"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:text="Settings"
            android:textColor="@color/white"
            android:textSize="22sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Account"
            android:textColor="@color/brand_sapphire_blue"
            android:textSize="14sp"
            android:textAllCaps="true"
            android:letterSpacing="0.1" />

        <include android:id="@+id/setNotification" layout="@layout/item_settings_row" />
        <include android:id="@+id/setPassword" layout="@layout/item_settings_row" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="More"
            android:textColor="@color/brand_sapphire_blue"
            android:textSize="14sp"
            android:textAllCaps="true"
            android:letterSpacing="0.1" />

        <include android:id="@+id/setLanguage" layout="@layout/item_settings_row" />
        <include android:id="@+id/setPrivacy" layout="@layout/item_settings_row" />
        <include android:id="@+id/setAbout" layout="@layout/item_settings_row" />

    </LinearLayout>
</androidx.core.widget.NestedScrollView>